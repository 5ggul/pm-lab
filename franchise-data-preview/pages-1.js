function home(){
  setTitle('프랜차이즈 창업 데이터 비교');
  const featured=brands.slice(0,8).map(brandCard).join('');
  return `<div class="page"><section class="hero"><div class="shell hero-grid"><div><span class="eyebrow">PUBLIC DATA × DECISION TOOL</span><h1>창업 전에,<br><em>숫자부터 비교</em>하세요.</h1><p class="hero-copy">프랜차이즈 창업비용·가맹점 현황·업종 내 위치를 한 화면에서 비교하고, 내 임대조건까지 넣어 실제 필요한 초기자금을 직접 계산합니다.</p><div class="search-wrap"><form class="hero-search" id="brandSearch"><input id="brandSearchInput" autocomplete="off" placeholder="브랜드명 검색 — 예: 메가커피, 교촌치킨" aria-label="브랜드 검색"><button class="primary-button">브랜드 찾기</button></form><div class="search-suggestions" id="searchSuggestions"></div></div><div class="hero-chips"><button class="chip" data-search="메가커피">메가커피</button><button class="chip" data-search="컴포즈">컴포즈커피</button><button class="chip" data-search="교촌">교촌치킨</button><button class="chip" data-search="맘스터치">맘스터치</button></div></div><div class="hero-panel"><div class="panel-top"><div><small>브랜드 예시</small><h2 class="panel-title">메가MGC커피</h2></div><span class="demo-badge">PREVIEW DATA</span></div><div class="metric-grid"><div class="metric-mini"><span>창업 관련 공개비용</span><b>${won(brands[0].cost)}</b></div><div class="metric-mini"><span>가맹점 수</span><b>${num(brands[0].stores)}개</b></div><div class="metric-mini"><span>전년 대비</span><b>+${pct(derived(brands[0]).yoy)}</b></div><div class="metric-mini"><span>카페 내 매장수 위치</span><b>상위 ${Math.round(100-derived(brands[0]).storePercentile)}%</b></div></div><div class="panel-insight">동종 카페 브랜드의 샘플 중앙값보다 매장 수가 <b>${Math.round(derived(brands[0]).storeVs)}% 많고</b>, 공개비용 예시는 <b>${Math.abs(Math.round(derived(brands[0]).costVs))}% ${derived(brands[0]).costVs<0?'낮습니다':'높습니다'}.</b></div><div class="panel-source"><span>샘플 기준연도 2025</span><span>공식 API 연결 전 UI 검수용</span></div></div></div></section>
  <section class="section"><div class="shell"><div class="section-head"><div><span class="eyebrow">BRANDS</span><h2>먼저 많이 비교할 브랜드부터</h2><p>비용 숫자만 보여주지 않고 업종 중앙값과 매장 증감을 같이 봅니다.</p></div><a class="text-link" href="#/brands">전체 브랜드 →</a></div><div class="card-grid">${featured}</div></div></section>
  <section class="section soft"><div class="shell"><div class="section-head"><div><span class="eyebrow">TOOLS</span><h2>숫자를 내 조건으로 바꾸는 도구</h2><p>공식 공개값과 사용자 입력값을 구분해 계산 결과를 만듭니다.</p></div></div><div class="tool-cards"><article class="tool-card"><span class="tool-icon">↔</span><h3>브랜드 1:1 비교</h3><p>창업 관련 비용, 가맹점 수, 매출 관련 공개지표를 같은 기준으로 비교합니다.</p><a class="text-link" href="#/compare">지금 비교하기 →</a></article><article class="tool-card"><span class="tool-icon">₩</span><h3>창업비용 계산기</h3><p>보증금·권리금·운전자금까지 더해 실제 준비해야 할 초기자금을 계산합니다.</p><a class="text-link" href="#/tools/startup-cost">계산하기 →</a></article><article class="tool-card"><span class="tool-icon">◎</span><h3>상권 밀도 분석</h3><p>지역별 업소 수와 업종 비율을 비교해 ‘얼마나 몰려 있는지’를 숫자로 확인합니다.</p><a class="text-link" href="#/areas">지역 보기 →</a></article></div></div></section>
  <section class="section"><div class="shell"><div class="section-head"><div><span class="eyebrow">DATA QUALITY</span><h2>숫자보다 먼저 보여주는 것</h2><p>출처, 기준일, 계산 방식이 보이지 않으면 숫자도 믿기 어렵습니다.</p></div></div><div class="data-strip"><div><small>공식 원천</small><b>공정위 FairData</b></div><div><small>상권 원천</small><b>소상공인 상가정보</b></div><div><small>자체 가치</small><b>중앙값·증감·밀도</b></div><div><small>검수 정책</small><b>누락값 ≠ 0원</b></div></div></div></section></div>`;
}

function brandsPage(){
  setTitle('프랜차이즈 브랜드 찾기');
  return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'브랜드'}])}<div class="page-head"><div><span class="eyebrow">BRAND DIRECTORY</span><h1>프랜차이즈 브랜드</h1><p>업종별로 브랜드를 찾고 창업 관련 비용, 가맹점 수, 전년 대비 변화를 같은 화면에서 비교합니다.</p></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="panel"><div class="form-grid"><div class="field"><label>브랜드 검색</label><input id="listSearch" placeholder="브랜드명·법인명·별칭"></div><div class="field"><label>업종</label><select id="categoryFilter"><option value="all">전체 업종</option>${Object.entries(categories).map(([k,v])=>`<option value="${k}">${v.name}</option>`).join('')}</select></div></div></div><div class="section-head" style="margin-top:28px"><div><h2 style="font-size:24px">브랜드 <span id="brandCount">${brands.length}</span>개</h2><p>현재 프리뷰는 대표 브랜드 10개 fixture로 기능을 검수합니다.</p></div></div><div class="card-grid" id="brandGrid">${brands.map(brandCard).join('')}</div></div></div>`;
}

function brandPage(slug){
  const b=bySlug(slug),d=derived(b), peers=catBrands(b.categorySlug).filter(x=>x.slug!==b.slug).sort((a,c)=>c.stores-a.stores).slice(0,4);
  setTitle(`${b.name} 창업비용·가맹 현황`);
  const maxRegion=Math.max(...Object.values(b.regions));
  return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'브랜드',href:'/brands'},{label:b.name}])}<div class="page-head"><div><span class="eyebrow">${b.category.toUpperCase()} · BRAND DATA</span><h1>${esc(b.name)} 창업비용과 가맹 현황</h1><p>${esc(b.corp)}의 공개 데이터 구조를 기준으로 비용·가맹점·업종 내 상대 위치를 한 화면에서 읽도록 설계한 상세 페이지입니다.</p></div><div class="page-actions"><a class="outline-button" href="#/compare/${b.slug}/${peers[0]?.slug||brands[1].slug}">비교하기</a><a class="primary-button" href="#/tools/startup-cost?brand=${b.slug}">내 조건 계산</a></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="summary-grid"><div class="summary-card"><span>창업 관련 공개비용 예시</span><b>${won(b.cost)}</b><small>임대차 비용 별도 · 샘플</small></div><div class="summary-card"><span>가맹점 예시</span><b>${num(b.stores)}개</b><small>직영점 ${num(b.direct)}개</small></div><div class="summary-card"><span>매출 관련 공개지표 예시</span><b>${won(b.sales)}</b><small>연 단위 샘플값</small></div><div class="summary-card"><span>전년 대비 가맹점</span><b class="${d.yoy>=0?'positive':'negative'}">${d.yoy>=0?'+':''}${pct(d.yoy)}</b><small>${num(b.lastStores)} → ${num(b.stores)}</small></div></div><div class="insight-banner"><p><b>30초 데이터 요약.</b> ${b.category} 동종 샘플 중앙값과 비교하면 공개비용은 <b>${Math.abs(d.costVs).toFixed(1)}% ${d.costVs<0?'낮고':'높고'}</b>, 가맹점 수는 <b>${Math.abs(d.storeVs).toFixed(1)}% ${d.storeVs>=0?'많습니다':'적습니다'}.</b> 이 문장은 공식 원본값이 아니라 원본을 가공한 파생 설명으로 분리 표시됩니다.</p><span class="insight-label">자체 계산</span></div><div class="two-col"><div class="stack"><section class="panel"><h2>창업비용 구성</h2><p class="panel-sub">공개 가능한 비용 항목만 더하며, 없는 값은 0으로 취급하지 않습니다.</p><div class="bar-list">${[['가맹비',b.fee],['교육비',b.education],['보증금',b.deposit],['인테리어',b.interior],['기타 공개비용',b.other]].map(([l,v])=>`<div class="bar-row"><span class="bar-label">${l}</span><span class="bar-track"><span class="bar-fill" style="width:${Math.max(4,v/Math.max(b.cost,b.interior)*100)}%"></span></span><span class="bar-value">${won(v)}</span></div>`).join('')}</div>${sourceLine()}</section><section class="panel"><h2>가맹점 변화</h2><p class="panel-sub">현재 샘플 스냅샷과 전년도 스냅샷을 보존해 증감 자체가 콘텐츠가 되도록 합니다.</p><div class="data-table-wrap"><table class="data-table"><thead><tr><th>구분</th><th class="num">이전 스냅샷</th><th class="num">현재 스냅샷</th><th class="num">변화</th></tr></thead><tbody><tr><td>가맹점</td><td class="num">${num(b.lastStores)}개</td><td class="num"><b>${num(b.stores)}개</b></td><td class="num"><span class="trend ${d.yoy>=0?'up':'down'}">${d.yoy>=0?'▲':'▼'} ${pct(Math.abs(d.yoy))}</span></td></tr><tr><td>직영점</td><td class="num">—</td><td class="num">${num(b.direct)}개</td><td class="num">정보 없음</td></tr><tr><td>가맹점 비율</td><td class="num">—</td><td class="num"><b>${pct(d.ratio)}</b></td><td class="num">자체 계산</td></tr></tbody></table></div>${sourceLine()}</section><section class="panel"><h2>지역별 매장 분포 예시</h2><p class="panel-sub">실제 서비스에서는 상권 좌표 데이터와 결합해 지역 페이지로 연결합니다.</p><div class="bar-list">${Object.entries(b.regions).map(([k,v])=>`<div class="bar-row"><span class="bar-label">${k}</span><span class="bar-track"><span class="bar-fill" style="width:${v/maxRegion*100}%"></span></span><span class="bar-value">${num(v)}개</span></div>`).join('')}</div>${sourceLine('FairData + 상권정보 결합 예정')}</section><section class="panel"><h2>${b.category} 브랜드 샘플 비교</h2><p class="panel-sub">동일 업종 안에서 상대 위치를 읽도록 중앙값과 백분위를 계산합니다.</p><div class="data-table-wrap"><table class="data-table"><thead><tr><th>브랜드</th><th class="num">공개비용</th><th class="num">가맹점</th><th class="num">전년 대비</th></tr></thead><tbody>${catBrands(b.categorySlug).sort((a,c)=>c.stores-a.stores).map(x=>{const dx=derived(x);return `<tr><td><a class="table-link" href="#/brand/${x.slug}">${esc(x.name)}</a></td><td class="num">${won(x.cost)}</td><td class="num">${num(x.stores)}개</td><td class="num"><span class="trend ${dx.yoy>=0?'up':'down'}">${dx.yoy>=0?'+':''}${pct(dx.yoy)}</span></td></tr>`}).join('')}</tbody></table></div>${sourceLine('동종 브랜드 fixture + 파생지표')}</section></div><aside class="stack side-card"><section class="panel"><h3>업종 내 상대 위치</h3><p class="panel-sub">${b.category} fixture ${catBrands(b.categorySlug).length}개 기준</p><div class="fact-list"><div class="fact-row"><span>비용 효율 위치</span><b>상위 ${Math.max(1,Math.round(100-d.costPercentile))}%</b></div><div class="fact-row"><span>매장 수 위치</span><b>상위 ${Math.max(1,Math.round(100-d.storePercentile))}%</b></div><div class="fact-row"><span>업종 비용 중앙값</span><b>${won(d.medCost)}</b></div><div class="fact-row"><span>업종 매장 중앙값</span><b>${num(d.medStores)}개</b></div></div></section><section class="cta-box"><h3>공개비용이 실제 준비자금은 아닙니다.</h3><p>임대보증금·권리금·추가 공사·운전자금을 더해 내 조건의 초기 필요자금을 계산해 보세요.</p><a class="primary-button" href="#/tools/startup-cost?brand=${b.slug}">내 조건으로 계산</a></section><section class="panel"><h3>함께 비교할 브랜드</h3><div class="related-list">${peers.map(x=>`<a class="related-item" href="#/compare/${b.slug}/${x.slug}"><b>${esc(x.name)}</b><span>${won(x.cost)} →</span></a>`).join('')}</div></section><section class="panel"><h3>데이터 상태</h3><div class="fact-list"><div class="fact-row"><span>화면 데이터</span><b>PREVIEW FIXTURE</b></div><div class="fact-row"><span>기준연도</span><b>샘플 2025</b></div><div class="fact-row"><span>최근 검수일</span><b>${PREVIEW_DATE}</b></div><div class="fact-row"><span>원본 Adapter</span><b>연결 전</b></div></div><a class="text-link" href="#/methodology">계산·검수 기준 보기 →</a></section></aside></div></div></div>`;
}
