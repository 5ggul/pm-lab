(()=>{const root=document.querySelector('[data-contract-checklist]');if(!root)return;const key='interior-v6-contract-checklist';const boxes=[...root.querySelectorAll('[data-check-id]')],out=root.querySelector('[data-check-progress]'),saved=(()=>{try{return JSON.parse(localStorage.getItem(key))||{}}catch{return{}}})();boxes.forEach(b=>{b.checked=!!saved[b.dataset.checkId];b.addEventListener('change',sync)});root.querySelector('[data-check-reset]')?.addEventListener('click',()=>{boxes.forEach(b=>b.checked=false);sync()});root.querySelector('[data-check-print]')?.addEventListener('click',()=>window.print());function sync(){const state={};boxes.forEach(b=>state[b.dataset.checkId]=b.checked);try{localStorage.setItem(key,JSON.stringify(state))}catch{}const done=boxes.filter(b=>b.checked).length;if(out)out.textContent=`${done} / ${boxes.length}`}sync()})();