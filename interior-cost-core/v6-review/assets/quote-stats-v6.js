(()=>{
  const scriptUrl=document.currentScript?.src||location.href,root=new URL('../',scriptUrl),$=(s,r=document)=>r.querySelector(s),money=n=>Number(n||0).toLocaleString('ko-KR');
  let dataset={segments:[],minimum_public_sample:80,status:'no_public_segment'};
  const regions=['서울','경기','인천','부산','대구','광주','대전','울산','세종','강원','충북','충남','전북','전남','경북','경남','제주','기타'];
  if(!document.querySelector('link[data-v6-quote-stats]')){const l=document.createElement('link');l.rel='stylesheet';l.href=new URL('quote-stats-v6.css',scriptUrl).href;l.dataset.v6QuoteStats='';document.head.append(l)}
  function savedScenario(){try{return JSON.parse(localStorage.getItem('interior-v6-scenario'))||{}}catch{return{}}}
  function saveScenario(){const data={};document.querySelectorAll('.filter-row select').forEach(x=>data[x.id]=x.value);try{localStorage.setItem('interior-v6-scenario',JSON.stringify(data))}catch{}}
  function addSelect(id,label,options,defaultValue){const row=$('.filter-row');if(!row||$('#'+id))return;const el=document.createElement('label');el.innerHTML=`<span>${label}</span><select id="${id}">${options.map(([v,t])=>`<option value="${v}">${t}</option>`).join('')}</select>`;row.append(el);$('#'+id).value=savedScenario()[id]||defaultValue}
  function bootstrapFilters(){
    const region=$('#region');if(region){const current=savedScenario().region||region.value;region.innerHTML=regions.map(v=>`<option value="${v}">${v}</option>`).join('');region.value=regions.includes(current)?current:'서울'}
    addSelect('bathrooms','욕실 수',[['0','0개'],['1','1개'],['2','2개'],['3+','3개 이상']],'2');
    addSelect('vat-condition','VAT',[['포함','포함'],['별도','별도'],['미확인','미확인']],'미확인');
    addSelect('waste-condition','폐기물',[['포함','포함'],['별도','별도'],['미확인','미확인']],'미확인');
  }
  function bootstrapMarket(){
    const metric=$('.metric.status');if(metric){metric.dataset.quoteStatMetric='';const strong=$('strong',metric),small=$('small',metric);if(strong)strong.dataset.quoteStatStatus='';if(small)small.dataset.quoteStatSmall=''}
    const panel=$('.market-empty');if(panel&&!panel.dataset.marketPanel){panel.dataset.marketPanel='';panel.innerHTML=`<div><strong>내 견적 vs 민간 분포</strong><span data-market-status>비교 데이터 공개 기준 미충족</span></div><div class="market-conditions" data-market-conditions></div><div class="range-placeholder" data-market-placeholder aria-hidden="true"><i></i><i></i><i></i></div><div class="market-range-wrap" data-market-range-wrap hidden><svg class="market-range-svg" data-market-range role="img" aria-label="익명 견적 P25 중앙값 P75와 내 견적 비교"></svg><div class="market-range-labels" data-market-labels></div></div><p class="market-range-note" data-market-note>같은 조건의 검수 승인 유효표본이 80건 이상일 때만 P25·중앙값·P75를 표시합니다.</p>`}
  }
  const pyeongBand=v=>({'24평':'20-24평','30평':'30-34평','32평':'30-34평','34평':'30-34평','40평':'40-49평'})[v]||v,scope=v=>String(v||'').startsWith('전체')?'전체':'부분',windows=v=>v==='포함'?'included':'excluded',condition=v=>v==='포함'?'included':v==='별도'?'separate':'unknown';
  function selected(){return{region_level1:$('#region')?.value||'',pyeong_band:pyeongBand($('#size')?.value||''),scope:scope($('#scope')?.value||''),bathroom_count:$('#bathrooms')?.value||'2',window_status:windows($('#window')?.value||'제외'),vat_status:condition($('#vat-condition')?.value||'미확인'),waste_status:condition($('#waste-condition')?.value||'미확인')}}
  const same=(a,b)=>Object.entries(a).every(([k,v])=>String(b?.[k]??'')===String(v));
  const findSegment=()=>{const wanted=selected();return(dataset.segments||[]).find(s=>same(wanted,s.dimensions))||null};
  function chips(){const host=$('[data-market-conditions]');if(!host)return;const x=selected(),bath=x.bathroom_count==='3+'?'3개 이상':`${x.bathroom_count}개`;host.innerHTML=[`지역 ${x.region_level1}`,`평수 ${x.pyeong_band}`,`범위 ${x.scope}`,`욕실 ${bath}`,`샷시 ${$('#window')?.value||'제외'}`,`VAT ${$('#vat-condition')?.value||'미확인'}`,`폐기물 ${$('#waste-condition')?.value||'미확인'}`].map(v=>`<span>${v}</span>`).join('')}
  function scenarioTitle(){const el=$('#scenario-title');if(!el)return;const b=$('#bathrooms')?.value||'2';el.textContent=[$('#size')?.value,$('#region')?.value,$('#scope')?.value?.replace(' 인테리어',''),`욕실 ${b==='3+'?'3개 이상':b+'개'}`,`샷시 ${$('#window')?.value||'제외'}`,`VAT ${$('#vat-condition')?.value||'미확인'}`,`폐기물 ${$('#waste-condition')?.value||'미확인'}`].filter(Boolean).join(' · ')}
  function svg(tag,attrs={}){const e=document.createElementNS('http://www.w3.org/2000/svg',tag);Object.entries(attrs).forEach(([k,v])=>e.setAttribute(k,v));return e}
  function drawRange(seg){const chart=$('[data-market-range]');if(!chart)return;const s=seg.total_amount_manwon||{},input=Number($('#quote-total')?.value||0),vals=[s.p25,s.median,s.p75,input||null].filter(v=>Number.isFinite(Number(v))).map(Number);if(vals.length<3){chart.hidden=true;return}const rawMin=Math.min(...vals),rawMax=Math.max(...vals),pad=Math.max(250,(rawMax-rawMin)*.18),min=Math.max(0,rawMin-pad),max=rawMax+pad,x=v=>48+(Number(v)-min)/(max-min)*704;chart.innerHTML='';chart.hidden=false;chart.setAttribute('viewBox','0 0 800 110');chart.append(svg('line',{x1:48,y1:56,x2:752,y2:56,stroke:'#c9c2b7','stroke-width':2}),svg('rect',{x:x(s.p25),y:43,width:Math.max(2,x(s.p75)-x(s.p25)),height:26,fill:'#e6ded0'}),svg('line',{x1:x(s.median),y1:34,x2:x(s.median),y2:78,stroke:'#171816','stroke-width':2}));[['P25',s.p25],['중앙값',s.median],['P75',s.p75]].forEach(([label,val])=>{const t=svg('text',{x:x(val),y:22,'text-anchor':'middle',fill:'#625e57','font-size':11});t.textContent=label;chart.append(t)});if(input){chart.append(svg('circle',{cx:x(input),cy:56,r:7,fill:'#a52b20',stroke:'#fcfbf7','stroke-width':2}));const t=svg('text',{x:x(input),y:101,'text-anchor':'middle',fill:'#a52b20','font-size':11,'font-weight':700});t.textContent='내 견적';chart.append(t)}}
  function render(){chips();scenarioTitle();const seg=findSegment(),metric=$('[data-quote-stat-metric]'),status=$('[data-quote-stat-status]'),small=$('[data-quote-stat-small]'),marketStatus=$('[data-market-status]'),placeholder=$('[data-market-placeholder]'),wrap=$('[data-market-range-wrap]'),note=$('[data-market-note]'),labels=$('[data-market-labels]'),scenarioMarket=$('.scenario-list div:first-child dd');if(!seg){if(metric)metric.dataset.quoteStatState='locked';if(status)status.textContent='공개 기준 미충족';if(small)small.textContent='선택 조건 공개 세그먼트 없음';if(marketStatus)marketStatus.textContent='비교 데이터 공개 기준 미충족';if(placeholder)placeholder.hidden=false;if(wrap)wrap.hidden=true;if(labels)labels.innerHTML='';if(note)note.textContent=`같은 조건의 검수 승인 유효표본이 ${dataset.minimum_public_sample||80}건 이상일 때만 P25·중앙값·P75를 표시합니다.`;if(scenarioMarket)scenarioMarket.textContent='미공개';return}if(metric)metric.dataset.quoteStatState='ready';if(status)status.textContent=`${money(seg.sample_count)}건`;if(small)small.textContent=`${seg.period?.from||'—'} ~ ${seg.period?.to||'—'} · 이상치 ${money(seg.excluded_outlier_count||0)}건 분리`;if(marketStatus)marketStatus.textContent=`공개 표본 ${money(seg.sample_count)}건`;if(placeholder)placeholder.hidden=true;if(wrap)wrap.hidden=false;if(scenarioMarket)scenarioMarket.textContent=`표본 ${money(seg.sample_count)}건`;const s=seg.total_amount_manwon||{},input=Number($('#quote-total')?.value||0);if(labels)labels.innerHTML=[['P25',s.p25],['중앙값',s.median],['P75',s.p75],['내 견적',input||null]].map(([k,v])=>`<div><span>${k}</span><strong>${v==null?'미입력':money(v)+'만원'}</strong></div>`).join('');if(note)note.textContent=`${seg.period?.from||'—'}~${seg.period?.to||'—'} 익명 견적 중 같은 조건의 검수 승인 표본만 집계했습니다. 이 범위는 적정가 판정이 아닙니다.`;drawRange(seg)}
  async function load(){try{const r=await fetch(new URL('data/quote-public-segments.json',root),{cache:'no-store'});if(r.ok){const d=await r.json();if(Array.isArray(d?.segments))dataset=d}}catch{}render()}
  bootstrapFilters();bootstrapMarket();
  $('.filter-row')?.addEventListener('change',()=>{saveScenario();render()});$('#quote-total')?.addEventListener('input',render);load();
})();
