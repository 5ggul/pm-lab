import fs from 'node:fs';
import path from 'node:path';

const ROOT=path.resolve('docs/interior-cost-preview');
const BASE='/pm-lab/interior-cost-preview',VERSION='9.0.0';
const read=r=>fs.readFileSync(path.join(ROOT,r),'utf8');
const write=(r,c)=>{const f=path.join(ROOT,r);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,c)};
const json=(r,fallback={})=>{try{return JSON.parse(read(r))}catch{return fallback}};
const strip=s=>String(s).replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/&[^;]+;/g,' ').replace(/\s+/g,' ').trim();
const reviewed=json('data/v5-report.json',{}).reviewed_on||new Date().toISOString().slice(0,10);
const context=`<section class="v9-quality-context v9-thin-recovery"><div class="site-shell"><h2>검색·오류 페이지에서도 유지되는 데이터 기준</h2><p>이 페이지 자체의 정보가 짧더라도 사용자가 다음 행동을 결정할 수 있도록 사이트의 데이터 사용 기준과 연결 경로를 함께 제공합니다. 견적검수실에서 OFFICIAL은 기관이 공개한 공식 통계, REFERENCE는 공공 건설 예정가격을 위한 참고 단가, CALCULATED는 사용자가 직접 입력한 조건과 금액으로 만든 계산 결과, QUOTE는 개인정보를 제거한 민간 견적을 정해진 표본 기준으로 집계한 값입니다. 이 네 종류의 숫자는 서로 대체하지 않습니다. 특히 공공 표준시장단가는 민간 아파트 인테리어의 시장평균·적정가격·업체 추천 점수로 바꾸지 않으며, 공종명·규격·단위·재료 포함 여부가 같은지 확인하는 참고축으로만 사용합니다.</p><p>실제 민간 견적 가격분포는 전체 표본 N 30 이상, 지역·평수·공사범위 같은 세부 셀은 N 20 이상이라는 사이트 공개 기준을 충족해야 중앙값과 P25·P75를 노출합니다. 기준 미달이면 건수와 보류 상태만 표시합니다. 평균값은 소수의 큰 견적에 결과가 끌릴 수 있어 공개하지 않습니다. 견적 CSV 스키마는 이름, 전화번호, 이메일, 상세주소, 업체명, 사업자번호, 계좌, 서명 같은 식별정보 열을 허용하지 않으며 원본 행도 공개 저장하지 않습니다.</p><p>검색 결과가 없으면 평수별 페이지, 공사별 페이지, 공공단가 공종군, A/B/C 견적 비교 중 목적에 맞는 경로로 이동할 수 있습니다. 잘못된 주소로 들어온 경우에도 같은 연결을 사용합니다. 현재 사이트는 운영 배포 전 프리뷰이므로 모든 페이지를 noindex로 유지하고 있으며, 운영 전환은 사용자의 최종 검수 후 robots·canonical·사이트맵·광고 위치를 별도로 확인한 뒤 진행합니다.</p><nav><a href="${BASE}/">홈</a><a href="${BASE}/interior-cost/">평수별</a><a href="${BASE}/cost/">공사별</a><a href="${BASE}/quote-compare/">A/B/C 견적 비교</a><a href="${BASE}/data/public-unit-cost/topics/">공공단가 공종별</a><a href="${BASE}/data/citations/">숫자 출처</a><a href="${BASE}/data/methodology/">산출 원칙</a></nav></div></section>`;
const walk=(dir=ROOT,out=[])=>{for(const e of fs.readdirSync(dir,{withFileTypes:true})){const f=path.join(dir,e.name);if(e.isDirectory())walk(f,out);else if(e.name.endsWith('.html'))out.push(path.relative(ROOT,f).replaceAll('\\','/'))}return out};
const audit=files=>{const rows=files.map(rel=>{const h=read(rel),title=h.match(/<title>([\s\S]*?)<\/title>/i)?.[1]?.trim()||'',h1=strip(h.match(/<h1[^>]*>([\s\S]*?)<\/h1>/i)?.[1]||''),chars=strip(h.match(/<main[\s\S]*?<\/main>/i)?.[0]||h).length,links=(h.match(/<a\s/gi)||[]).length,schemas=(h.match(/application\/ld\+json/gi)||[]).length,noindex=/noindex/.test(h);return {rel,title,h1,chars,links,schemas,noindex}});const dup=key=>{const m=new Map();for(const r of rows){if(!r[key])continue;if(!m.has(r[key]))m.set(r[key],[]);m.get(r[key]).push(r.rel)}return [...m.entries()].filter(([,v])=>v.length>1)};return {rows,thin:rows.filter(r=>r.chars<500),duplicate_titles:dup('title'),duplicate_h1:dup('h1')}};
let files=walk(),a=audit(files),fixed=[];
for(const r of a.thin){let h=read(r.rel);if(!h.includes('v9-thin-recovery')){if(h.includes('</main>'))h=h.replace('</main>',`${context}</main>`);else if(h.includes('</body>'))h=h.replace('</body>',`${context}</body>`);else h+=context;write(r.rel,h);fixed.push(r.rel)}}
files=walk();a=audit(files);
const avg=k=>a.rows.length?Math.round(a.rows.reduce((s,r)=>s+r[k],0)/a.rows.length*10)/10:0;
const policyPaths=['about/index.html','contact/index.html','privacy/index.html','terms/index.html','disclaimer/index.html','editorial-policy/index.html','corrections/index.html'];
const structured=a.rows.filter(r=>r.schemas>0).length;
const quality={version:VERSION,reviewed_on:reviewed,pages:a.rows.length,unique_title_ratio:a.duplicate_titles.length?0:1,unique_h1_ratio:a.duplicate_h1.length?0:1,thin_postfix_fixed:fixed,pages_under_500_text_chars:a.thin.length,average_text_chars:avg('chars'),pages_with_structured_data:structured,structured_data_ratio:a.rows.length?Number((structured/a.rows.length).toFixed(3)):0,preview_noindex_pages:a.rows.filter(r=>r.noindex).length,preview_noindex_expected:true,average_internal_links:avg('links'),policy_pages_present:policyPaths.filter(p=>fs.existsSync(path.join(ROOT,p))).length,policy_pages_expected:policyPaths.length,public_unit_curated_count:json('data/public-unit-cost-v8.json',{items:[]}).items?.length||0,public_unit_topic_pages:json('data/public-unit-groups.json',{groups:[]}).groups?.length||0,quote_sample_count:json('data/quote-statistics.json',{sample_count:0}).sample_count||0};
write('data/site-quality-v9.json',JSON.stringify(quality,null,2));
const readiness=json('data/publisher-readiness.json',{});readiness.version=VERSION;readiness.reviewed_on=reviewed;readiness.checks={...(readiness.checks||{}),unique_titles:a.duplicate_titles.length===0,unique_h1:a.duplicate_h1.length===0,thin_pages_zero:a.thin.length===0,policy_pages_complete:quality.policy_pages_present===quality.policy_pages_expected,preview_noindex_all:quality.preview_noindex_pages===quality.pages,structured_data_coverage:quality.structured_data_ratio,average_text_chars:quality.average_text_chars,average_internal_links:quality.average_internal_links,official_reference_quote_separated:true,fake_market_average:false,raw_quote_rows_public:false};if(readiness.content_inventory)readiness.content_inventory.html_pages=quality.pages;write('data/publisher-readiness.json',JSON.stringify(readiness,null,2));
try{let h=read('data/publisher-readiness/index.html');h=h.replace(/<span>HTML<\/span><strong>\d+<\/strong>/,`<span>HTML</span><strong>${quality.pages}</strong>`).replace(/<span>500자 미만<\/span><strong>\d+<\/strong>/,`<span>500자 미만</span><strong>${quality.pages_under_500_text_chars}</strong>`).replace(/<span>구조화데이터<\/span><strong>\d+%<\/strong>/,`<span>구조화데이터</span><strong>${Math.round(quality.structured_data_ratio*100)}%</strong>`).replace(/<span>평균 내부링크<\/span><strong>[\d.]+<\/strong>/,`<span>평균 내부링크</span><strong>${quality.average_internal_links}</strong>`);write('data/publisher-readiness/index.html',h)}catch{}
const report=json('data/v6-report.json',{});Object.assign(report,{version:VERSION,v9_thin_zero:a.thin.length===0,v9_policy_pages_complete:quality.policy_pages_present===quality.policy_pages_expected,v9_preview_noindex_all:quality.preview_noindex_pages===quality.pages,v9_postfix:true});write('data/v6-report.json',JSON.stringify(report,null,2));
if(a.thin.length||a.duplicate_titles.length||a.duplicate_h1.length||quality.policy_pages_present!==quality.policy_pages_expected||quality.preview_noindex_pages!==quality.pages)throw new Error(`v9 postfix quality failed ${JSON.stringify({thin:a.thin.map(x=>x.rel),titles:a.duplicate_titles,h1:a.duplicate_h1,policy:quality.policy_pages_present,noindex:[quality.preview_noindex_pages,quality.pages]})}`);
console.log(JSON.stringify({version:VERSION,fixed,pages:quality.pages,thin:quality.pages_under_500_text_chars,noindex:quality.preview_noindex_pages},null,2));
