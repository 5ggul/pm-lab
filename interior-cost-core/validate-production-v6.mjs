import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve(process.env.OUT_DIR||'/tmp/interior-v6-production');
const base=new URL(process.env.BASE_URL||'https://example.test/');
const errors=[];
function walk(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>{const f=path.join(dir,e.name);return e.isDirectory()?walk(f):[f]})}
const htmlFiles=walk(root).filter(f=>f.endsWith('.html'));
const titles=new Map(),descs=new Map();
function attr(html,selector,name){const re=name==='canonical'?/<link[^>]+rel=["']canonical["'][^>]+href=["']([^"']+)["']/i:new RegExp(`<meta[^>]+(?:name|property)=["']${selector}["'][^>]+content=["']([^"']*)["']`,'i');return (html.match(re)||[])[1]||''}
function targetFor(from,href){if(!href||href.startsWith('#')||/^(https?:|mailto:|tel:|javascript:)/i.test(href))return null;const clean=href.split('#')[0].split('?')[0];if(!clean)return null;const fromDir=path.dirname(path.relative(root,from));let rel=path.posix.normalize(path.posix.join(fromDir.replaceAll(path.sep,'/'),clean));if(clean.endsWith('/'))rel=path.posix.join(rel,'index.html');else if(!path.extname(rel))rel=path.posix.join(rel,'index.html');return path.join(root,...rel.split('/'))}
for(const file of htmlFiles){const rel=path.relative(root,file).replaceAll(path.sep,'/'),html=fs.readFileSync(file,'utf8'),is404=rel==='404.html';const title=(html.match(/<title>([\s\S]*?)<\/title>/i)||[])[1]?.trim()||'';const desc=attr(html,'description');if((html.match(/<h1\b/g)||[]).length!==1)errors.push(`h1:${rel}`);if(!title)errors.push(`title:${rel}`);if(!desc)errors.push(`description:${rel}`);if(title){if(titles.has(title))errors.push(`duplicate-title:${rel}:${titles.get(title)}`);else titles.set(title,rel)}if(desc){if(descs.has(desc))errors.push(`duplicate-description:${rel}:${descs.get(desc)}`);else descs.set(desc,rel)}if(is404){if(!/name=["']robots["'][^>]+noindex/i.test(html))errors.push('404-indexable');continue}if(/name=["']robots["'][^>]+noindex/i.test(html))errors.push(`noindex:${rel}`);const canonical=attr(html,'','canonical');if(!canonical||!canonical.startsWith(base.href))errors.push(`canonical:${rel}`);for(const key of ['og:title','og:description','og:url','twitter:card'])if(!attr(html,key))errors.push(`${key}:${rel}`);if(!html.includes('application/ld+json'))errors.push(`schema:${rel}`);const hrefs=[...html.matchAll(/href=["']([^"']+)["']/gi)].map(m=>m[1]);for(const href of hrefs){const target=targetFor(file,href);if(target&&!fs.existsSync(target))errors.push(`broken-link:${rel}->${href}`)}}
const sitemapPath=path.join(root,'sitemap.xml'),robotsPath=path.join(root,'robots.txt');if(!fs.existsSync(sitemapPath))errors.push('missing:sitemap.xml');if(!fs.existsSync(robotsPath))errors.push('missing:robots.txt');let sitemapCount=0;if(fs.existsSync(sitemapPath)){const sm=fs.readFileSync(sitemapPath,'utf8');sitemapCount=(sm.match(/<url>/g)||[]).length;if(sitemapCount!==htmlFiles.length-1)errors.push(`sitemap-count:${sitemapCount}/${htmlFiles.length-1}`);if(sm.includes('/404.html'))errors.push('sitemap-404')}if(fs.existsSync(robotsPath)){const r=fs.readFileSync(robotsPath,'utf8');if(!r.includes(`Sitemap: ${new URL('sitemap.xml',base).href}`))errors.push('robots-sitemap');if(!r.includes('Allow: /'))errors.push('robots-allow')}
if(errors.length){console.error(JSON.stringify({ok:false,errors:errors.slice(0,80),error_count:errors.length},null,2));process.exit(1)}console.log(JSON.stringify({ok:true,html_count:htmlFiles.length,indexable_count:htmlFiles.length-1,unique_titles:titles.size,unique_descriptions:descs.size,sitemap_count:sitemapCount,base_url:base.href},null,2));
