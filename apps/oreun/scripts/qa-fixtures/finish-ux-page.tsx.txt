import {randomUUID} from "node:crypto";
import {notFound} from "next/navigation";
import AnswerComposer from "@/components/AnswerComposer";
import DraftForm from "@/components/DraftForm";
import DeleteAccountForm from "@/components/DeleteAccountForm";
import type {WriteResult} from "@/lib/community/experience-model";
import type {DeletionResult} from "@/app/actions/account";
export const dynamic="force-dynamic";
async function simulate(data:FormData):Promise<WriteResult>{"use server";if(process.env.R1_COMMUNITY_UX_FIXTURE!=="1")throw new Error("fixture disabled");await new Promise(r=>setTimeout(r,750));const body=String(data.get("body")??data.get("title"));if(body.includes("실패"))return {status:"error",message:"QA 실패: 입력 유지"};if(body.includes("로그인"))return {status:"login",message:"QA 로그인 만료",href:"/login?next=%2Fqa-finish"};return {status:"success",message:"QA 성공",href:"/qa-finish?submitted=1"};}
async function erase(data:FormData):Promise<DeletionResult>{"use server";if(process.env.R1_COMMUNITY_UX_FIXTURE!=="1")throw new Error("fixture disabled");await new Promise(r=>setTimeout(r,750));return data.get("confirmation")==="탈퇴"&&data.get("acknowledged")==="on"?{status:"success",message:"QA ONLY"}:{status:"error",message:"확인 필요"};}
export default async function Fixture({searchParams}:{searchParams:Promise<{mode?:string;account?:string;scope?:string;submitted?:string}>}){if(process.env.R1_COMMUNITY_UX_FIXTURE!=="1")notFound();const q=await searchParams;const userId=q.account==="other"?"b4a744ad-0716-47f4-a7b0-333333333333":"b4a744ad-0716-47f4-a7b0-111111111111";const scope=q.scope??"b4a744ad-0716-47f4-a7b0-444444444444";const mode=q.mode??"answer";return <main className="page community-page"><h1>{q.submitted?"QA 성공":"로컬 상호작용 QA"}</h1><p>실제 계정·게시물·탈퇴 API를 호출하지 않는 테스트입니다.</p>{!q.submitted&&(mode==="delete"?<DeleteAccountForm userId={userId} action={erase}/>:mode==="answer"?<AnswerComposer key={userId+scope} userId={userId} questionId={scope} initialRequestId={randomUUID()} action={simulate}/>:<DraftForm key={userId+scope+mode} userId={userId} scope={scope} kind={mode==="party"?"party":"comment"} initialRequestId={randomUUID()} fields={mode==="party"?["title","note","playstyle"]:["body"]} action={simulate} submitLabel="등록하기">{mode==="party"?<><label>제목<input name="title" minLength={5} maxLength={100} required/></label><label>설명<textarea name="note" maxLength={1000}/></label><label>방식<select name="playstyle" defaultValue="casual"><option value="casual">가볍게</option><option value="learning">배우기</option></select></label></>:<label>댓글<input name="body" minLength={2} maxLength={1500} required/></label>}</DraftForm>)}</main>}
