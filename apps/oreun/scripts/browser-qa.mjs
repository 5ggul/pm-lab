import { chromium } from "playwright";
const base=process.env.QA_BASE_URL||"http://127.0.0.1:3000";const widths=[360,375,390,430];const browser=await chromium.launch({headless:true});const failures=[];
async function checkWidth(width){const page=await browser.newPage({viewport:{width,height:900}});const errors=[];page.on("console",m=>{if(m.type()==="error")errors.push(m.text())});page.on("pageerror",e=>errors.push(e.message));await page.goto(base,{waitUntil:"networkidle"});const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);if(overflow)failures.push(`${width}px horizontal overflow`);if(errors.length)failures.push(`${width}px console: ${errors.join(" | ")}`);await page.screenshot({path:`qa-home-${width}.png`,fullPage:true});await page.close();}
for(const width of widths)await checkWidth(width);
const page=await browser.newPage({viewport:{width:390,height:900}});const errors=[];page.on("console",m=>{if(m.type()==="error")errors.push(m.text())});page.on("pageerror",e=>errors.push(e.message));await page.goto(base,{waitUntil:"networkidle"});const input=page.locator("main").getByPlaceholder(/게임 이름/);await input.fill("라이벌즈");await input.press("Enter");await page.waitForURL(/\/game\/rivals/);if(!(await page.getByRole("link",{name:/Roblox에서 플레이/}).isVisible()))failures.push("play link missing");const seven=page.getByRole("button",{name:/7D/});if(await seven.isEnabled())await seven.click();await page.screenshot({path:"qa-rivals-390.png",fullPage:true});if(errors.length)failures.push(`flow console: ${errors.join(" | ")}`);await page.close();
const desktop=await browser.newPage({viewport:{width:1365,height:900}});await desktop.goto(`${base}/rising`,{waitUntil:"networkidle"});await desktop.screenshot({path:"qa-rising-desktop.png",fullPage:true});await desktop.close();await browser.close();if(failures.length){console.error(JSON.stringify({ok:false,failures},null,2));process.exit(1)}console.log(JSON.stringify({ok:true,widths,flow:"home -> search rivals -> game -> 7D",base},null,2));
