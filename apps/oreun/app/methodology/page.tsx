import type {Metadata} from "next";import Header from "@/components/Header";import {getGameCatalog} from "@/lib/catalog";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"데이터·급상승 산정 기준", alternates: { canonical: "/methodology" }};
export default async function Methodology(){const games=await getGameCatalog();return <><Header games={games}/><main className="page methodology"><div className="page-title"><h1>산정 기준</h1><p>오름이 숫자를 가져오고 계산하고 숨기는 기준입니다.</p></div><div className="callout"><strong>현재값과 오름 계산값은 다릅니다.</strong><br/>현재 플레이 인원은 Roblox 공개 경험 API를 우선 사용하고, 변화율·Trend는 오름이 저장한 Snapshot에서 계산합니다.</div><h2>데이터 출처</h2><p>Sprint 01의 1차 Provider는 Roblox Creator Hub에 문서화된 <code>games.roblox.com/v1/games</code> 공개 경험 상세 API입니다. UI는 외부 API를 직접 호출하지 않고 Provider Adapter가 내부 모델로 변환합니다.</p><h2>Freshness</h2><ul><li><b>정상 갱신</b>: 기대 수집 주기 안에서 최근 응답을 확보했습니다.</li><li><b>갱신 지연</b>: 마지막 정상 Snapshot은 있으나 목표 시각을 넘겼습니다.</li><li><b>오래된 데이터</b>: 현재값으로 신뢰하기 어렵습니다.</li><li><b>데이터 없음</b>: 정상 Snapshot이 한 번도 없습니다.</li></ul><p>0명과 데이터 없음은 다른 상태입니다.</p><h2>Historical Data</h2><p>Raw Snapshot은 단기 보관하고 Hourly·Daily Rollup으로 축약합니다. 누락 구간은 0으로 연결하지 않습니다. 7일 지표의 커버리지가 부족하면 7일 증감률 대신 ‘데이터 수집 중’을 표시합니다.</p><h2>Trend Engine v1</h2><p>Popularity(현재 절대 플레이 인원)와 Trending(상승 모멘텀)을 분리합니다.</p><ul><li>절대 모멘텀 30%</li><li>상대 성장 25%</li><li>기준 플레이 규모 15%</li><li>데이터 커버리지 10%</li><li>업데이트 신선도 10%</li><li>내부 관심 10% — 충분한 고유 사용자 데이터가 생기기 전에는 비활성화하고 나머지 가중치를 재정규화</li></ul><p>상대 성장률에는 baseline floor와 clipping을 적용합니다. 1명→20명처럼 비율만 큰 변화가 10만명 규모 게임의 의미 있는 상승을 무조건 이기지 않게 합니다.</p><h2>Preview Fixture</h2><p><code>R1_PREVIEW_FIXTURES=1</code>은 차트와 Trend UI를 검수하기 위한 개발 전용 과거 시계열입니다. 켜진 경우 모든 화면 상단에 명확하게 표시하며 운영 데이터로 사용하지 않습니다.</p></main></>}
