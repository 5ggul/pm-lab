import type {Metadata} from "next";import Header from "@/components/Header";import {getGameCatalog} from "@/lib/catalog";import {previewFixtureEnabled} from "@/lib/history";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"데이터 상태 · 내부 Preview",robots:{index:false,follow:false}};
export default async function DataStatus(){const games=await getGameCatalog();const counts={live:games.filter(g=>g.sourceStatus==="live").length,fresh:games.filter(g=>g.freshnessState==="fresh").length,stale:games.filter(g=>g.freshnessState==="stale").length,unavailable:games.filter(g=>g.freshnessState==="unavailable").length};return <><Header games={games}/><main className="page"><div className="page-title"><h1>Data Status</h1><p>Preview 내부 상태 화면 · 검색엔진 색인 금지. 실제 운영에서는 관리자 인증 뒤로 이동합니다.</p></div><div className="status-grid"><div className="status-cell"><strong>{counts.live}</strong><span>live provider</span></div><div className="status-cell"><strong>{counts.fresh}</strong><span>fresh</span></div><div className="status-cell"><strong>{counts.stale}</strong><span>stale</span></div><div className="status-cell"><strong>{counts.unavailable}</strong><span>unavailable</span></div></div><div className="section-head"><h2>Collector 준비 상태</h2></div><p>Provider Adapter, partial failure, 429 분류, idempotent Snapshot Repository interface가 구현되어 있습니다. Preview는 아직 영구 DB에 연결하지 않았으므로 ingestion_runs의 실제 실행 이력은 없습니다.</p><div className="callout">Historical Fixture: <strong>{previewFixtureEnabled()?"ON · 개발용":"OFF · 기본값"}</strong></div></main></>}
