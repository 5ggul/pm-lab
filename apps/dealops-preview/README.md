# DealOps permanent Cloudflare deployment 0.6.6

Claimed service: `https://dealops-preview.obvious-chive.workers.dev`. Existing D1 data is preserved; no temporary-account provisioning is used by the active workflow.

The reproducible Cloudflare runtime source is stored as a SHA-256-verified xz JSON capsule in `.transfer/` (20 UTF-8 files). AI and collectors remain OFF. Daangn publishing remains manual-only.

Capsule SHA-256: `86f2a9f335a7b58ba9a407ec94d9077d32f102538c84cad68b163b4b22156ebb`

Version 0.6.6 changes the cafe-copy engine to factual Korean for prices, options, exclusions and conditions, removes forced cute endings, retains attributable real reviews only, and keeps recent-copy repetition checks.

Deployment on 2026-09-24 used the existing `dealops-preview` Worker and existing `dealops-preview` D1 database. A pre-deploy D1 SQL export was created on the authorized owner PC. Post-deploy health reports 0.6.6 and the existing administrator setup remains present.
