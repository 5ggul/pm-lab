import test from 'node:test';import assert from 'node:assert/strict';import {airportMeta} from '../src/airports.js';import {buildAirportAnswerBlock,topRoutesTodaySpec,airlineTodaySpec} from '../src/stats/airport-detail.js';
test('all 15 airport metadata includes ICAO',()=>{for(const c of ['ICN','GMP','CJU','PUS','TAE','CJJ','KWJ','USN','RSU','MWX','KPO','HIN','KUV','WJU','YNY'])assert.match(airportMeta(c).icao,/^RK[A-Z0-9]{2}$/)});
test('answer block is deterministic from visible numbers',()=>{const s=buildAirportAnswerBlock({airport:airportMeta('CJU'),hourKst:16,current:{delay_rate:8.7,eligible_flights:149,delayed_flights:13,cancelled_flights:2,data_as_of:'2026-09-03T07:41:00Z'},baseline:{baseline_delay_rate:3.4,baseline_sample_size:100}});assert.match(s,/8\.7%/);assert.match(s,/3\.4%/);assert.match(s,/5\.3%p/);assert.match(s,/149편/);assert.match(s,/13편/);assert.match(s,/2편/)});
test('answer block does not invent baseline with too-small sample',()=>{const s=buildAirportAnswerBlock({airport:airportMeta('CJU'),hourKst:16,current:{delay_rate:8.7,eligible_flights:10,delayed_flights:1,cancelled_flights:0},baseline:{baseline_delay_rate:3.4,baseline_sample_size:3}});assert.doesNotMatch(s,/3\.4%/)});
test('route and airline queries use current single-source table',()=>{assert.match(topRoutesTodaySpec({iata:'CJU',serviceDate:'2026-09-03'}).sql,/FROM flight_current/);assert.match(airlineTodaySpec({iata:'CJU',serviceDate:'2026-09-03'}).sql,/FROM flight_current/)})
