name: interior-post223-independent-qa

on:
  push:
    branches:
      - interior-post223-independent
    paths:
      - 'docs/interior-cost-preview/quote-check/**'
      - 'docs/interior-cost-preview/quote-compare/**'
      - 'tests/interior-post223-independent/**'
      - '.github/workflows/interior-post223-independent-qa.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: interior-post223-independent-qa-${{ github.ref }}
  cancel-in-progress: true

jobs:
  browser-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout independent QA branch
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Syntax check import assets
        run: |
          node --check docs/interior-cost-preview/assets/quote-local-import-v1.js
          node --check tests/interior-post223-independent/ci-browser-qa.cjs

      - name: Install isolated Playwright Chromium
        run: |
          mkdir -p /tmp/interior-p1-import-ci
          cd /tmp/interior-p1-import-ci
          npm init -y >/dev/null 2>&1
          npm install --no-audit --no-fund playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Start independent static server
        run: |
          ln -sfn docs pm-lab
          python3 -m http.server 4173 --bind 127.0.0.1 >/tmp/interior-p1-import-http.log 2>&1 &
          echo $! >/tmp/interior-p1-import-http.pid
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:4173/pm-lab/interior-cost-preview/quote-check/ >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/interior-p1-import-http.log || true
          exit 1

      - name: Start ephemeral HTTPS quick tunnel
        run: |
          curl -fsSL --retry 5 --retry-delay 2 -o /tmp/cloudflared             https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x /tmp/cloudflared
          /tmp/cloudflared tunnel --no-autoupdate --url http://127.0.0.1:4173 >/tmp/interior-p1-import-cloudflared.log 2>&1 &
          echo $! >/tmp/interior-p1-import-cloudflared.pid
          URL=''
          for i in $(seq 1 60); do
            URL=$(grep -Eo 'https://[-a-z0-9]+\.trycloudflare\.com' /tmp/interior-p1-import-cloudflared.log | head -n1 || true)
            if [ -n "$URL" ]; then break; fi
            sleep 1
          done
          if [ -z "$URL" ]; then
            cat /tmp/interior-p1-import-cloudflared.log || true
            exit 1
          fi
          echo "QA_BASE_URL=$URL" >> "$GITHUB_ENV"
          for i in $(seq 1 90); do
            if curl -fsS --connect-timeout 5 "$URL/pm-lab/interior-cost-preview/quote-check/" >/dev/null; then
              echo "HTTPS candidate ready"
              exit 0
            fi
            sleep 1
          done
          cat /tmp/interior-p1-import-cloudflared.log || true
          exit 1

      - name: Run post-223 independent browser QA
        env:
          PLAYWRIGHT_MODULE: /tmp/interior-p1-import-ci/node_modules/playwright
          QA_RESULT_PATH: interior-post223-independent-qa.json
        run: node tests/interior-post223-independent/ci-browser-qa.cjs

      - name: Write QA summary
        if: always()
        run: |
          {
            echo '## Interior post-223 independent QA'
            echo
            echo "- Commit: `$GITHUB_SHA`"
            if [ -n "$QA_BASE_URL" ]; then
              echo "- Ephemeral HTTPS origin: `$QA_BASE_URL`"
            fi
            if [ -f interior-post223-independent-qa.json ]; then
              node - <<'NODE'
          const fs=require('fs');
          const r=JSON.parse(fs.readFileSync('interior-post223-independent-qa.json','utf8'));
          console.log('- Assertions passed: `' + (r.passed ?? 0) + '`');
          console.log('- Failures: `' + (r.failed ?? 0) + '`');
          NODE
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload QA artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: interior-post223-independent-qa
          path: |
            interior-post223-independent-qa.json
            /tmp/interior-p1-import-http.log
            /tmp/interior-p1-import-cloudflared.log
          if-no-files-found: warn
          retention-days: 14
