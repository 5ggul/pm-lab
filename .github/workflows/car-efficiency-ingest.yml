name: car-efficiency-ingest

on:
  workflow_dispatch:
  schedule:
    - cron: '17 20 * * *'
  push:
    branches: [main]
    paths:
      - 'docs/car-data-preview/scripts/fetch-kea-cars.mjs'
      - 'docs/car-data-preview/scripts/match-kea-cars.mjs'
      - 'docs/car-data-preview/scripts/discover-kea-rich-source.mjs'
      - '.github/workflows/car-efficiency-ingest.yml'

permissions:
  contents: write

concurrency:
  group: car-efficiency-ingest
  cancel-in-progress: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Discover rich KEA file/API source
        continue-on-error: true
        run: node docs/car-data-preview/scripts/discover-kea-rich-source.mjs
      - name: Fetch all KEA vehicle rows
        id: fetch
        continue-on-error: true
        env:
          DATA_GO_KR_SERVICE_KEY: ${{ secrets.DATA_GO_KR_SERVICE_KEY }}
          KEA_CAR_API_ENDPOINT: ${{ vars.KEA_CAR_API_ENDPOINT }}
          KEA_CAR_SOURCE_PAGE: ${{ vars.KEA_CAR_SOURCE_PAGE }}
          KEA_CAR_CSV_URL: ${{ secrets.KEA_CAR_CSV_URL }}
        run: node docs/car-data-preview/scripts/fetch-kea-cars.mjs
      - name: Match API rows to reviewed SSOT
        if: always()
        run: node docs/car-data-preview/scripts/match-kea-cars.mjs
      - name: Commit sanitized API snapshot and coverage
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          FILES=(
            docs/car-data-preview/data/raw/kea-car-efficiency.json
            docs/car-data-preview/data/staging/kea-car-efficiency-normalized.json
            docs/car-data-preview/data/staging/kea-car-efficiency-unmatched.json
            docs/car-data-preview/data/staging/kea-model-candidates.json
            docs/car-data-preview/data/generated/kea-api-status.json
            docs/car-data-preview/data/generated/kea-api-coverage.json
            docs/car-data-preview/data/generated/kea-rich-source-discovery.json
          )
          EXISTING=()
          for p in "${FILES[@]}"; do
            if [ -e "$p" ]; then EXISTING+=("$p"); fi
          done
          if [ "${#EXISTING[@]}" -eq 0 ]; then
            echo "No KEA outputs to publish."
            exit 0
          fi
          tar -czf /tmp/car-api-output.tgz "${EXISTING[@]}"
          for attempt in 1 2 3; do
            echo "Publish attempt $attempt/3"
            git fetch origin main
            git reset --hard origin/main
            tar -xzf /tmp/car-api-output.tgz
            git add "${EXISTING[@]}"
            if git diff --cached --quiet; then
              echo "KEA snapshot/status already current."
              exit 0
            fi
            git commit -m "data: refresh KEA car efficiency snapshot [car-api]"
            if git push origin HEAD:main; then
              exit 0
            fi
            sleep $((attempt * 2))
          done
          echo "Could not publish KEA outputs after 3 fast-forward retries."
          exit 1
      - name: Report source access failure
        if: steps.fetch.outcome == 'failure'
        run: |
          cat docs/car-data-preview/data/generated/kea-api-status.json || true
          exit 1
