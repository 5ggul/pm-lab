name: collect-updown
on:
  schedule:
    - cron: "3,33 * * * *"
  workflow_dispatch:

# 취소 대신 대기: 취소되면 커밋 스텝까지 죽어서 수집분이 통째로 유실됨
concurrency:
  group: collect-updown
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 29
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests
      - name: run updown watcher (~22min)
        env:
          PRICE_SOURCE: coinbase
        run: |
          mkdir -p data
          cd data
          timeout 1320 python ../updown_watcher.py --assets btc,eth --windows 300,900 --poll 5 || true
      - name: commit data
        if: always()
        run: |
          git config user.name "pm-lab-bot"
          git config user.email "bot@users.noreply.github.com"
          git add data
          git diff --cached --quiet && exit 0
          git commit -m "data: updown $(date -u +%FT%TZ)"
          for i in 1 2 3; do
            git push && exit 0
            git pull --rebase --autostash
          done
          git push
