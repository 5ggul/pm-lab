name: interior-p1-local-import-production-smoke

on:
  push:
    branches:
      - interior-p1-local-import
    paths:
      - 'tests/interior-p1-local-import-production-smoke/**'
      - '.github/workflows/interior-p1-local-import-production-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: interior-p1-local-import-production-smoke-\${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout smoke branch
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install isolated Playwright Chromium
        run: |
          mkdir -p /tmp/interior-p1-import-prod
          cd /tmp/interior-p1-import-prod
          npm init -y >/dev/null 2>&1
          npm install --no-audit --no-fund playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Run public local import smoke
        env:
          PLAYWRIGHT_MODULE: /tmp/interior-p1-import-prod/node_modules/playwright
        run: node tests/interior-p1-local-import-production-smoke/production-smoke.cjs
