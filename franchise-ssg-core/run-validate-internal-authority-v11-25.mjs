import fs from 'node:fs/promises';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url)),out=path.resolve(here,'../docs/franchise-ssg-preview'),err=[];
const report=JSON.parse(await fs.readFile(path.join(out,'internal-authority-report.json'),'utf8')),quality=JSON.parse(await fs.readFile(path.join(out,'v11-quality-report.json'),'utf8')),manifest=JSON.parse(await fs.readFile(path.join(out,'route-manifest.json'),'utf8')),toolTrust=JSON.parse(await fs.readFile(path.join(out,'v11-15-tool-trust.json'),'utf8'));
const home=await fs.readFile(path.join(out,'index.html'),'utf8'),tools=await fs.readFile(path.join(out,'tools/index.html'),'utf8'),categories=await fs.readFile(path.join(out,'categories/index.html'),'utf8'),compare=await fs.readFile(path.join(out,'compare/index.html'),'utf8');
const candidates=new Set(quality.indexPolicy?.productionCandidateUrls||[]),graph=report.graph||{};
if(report.revision!=='2026-09-10-v11.25')err.push('revision');if(report.baseUiVersion!=='11.25'||manifest.uiVersion!=='11.25')err.push('uiVersion');if(report.productionCandidateCount!==184||candidates.size!==184)err.push('candidate count');
if(manifest.internalAuthorityPostpass?.revision!==report.revision||quality.internalAuthorityPostpass?.revision!==report.revision)err.push('metadata');if(manifest.internalAuthorityPostpass?.singleSnapshot!==true||quality.internalAuthorityPostpass?.singleSnapshot!==true)err.push('single snapshot');
if(!home.includes('data-v25-home="1"')||report.home?.terminalPreserved!==true)err.push('home terminal');if(home.includes('data-internal-authority-tools="1"')||home.includes('data-v11-home-funnel="1"'))err.push('legacy home injected');
for(const route of ['/brands/','/categories/','/compare/','/tools/','/explore/'])if(!home.includes(`href="/pm-lab/franchise-ssg-preview${route}"`))err.push('home hub '+route);
const approved=toolTrust.approvedToolRoutes||[];if(approved.length!==8||report.tools?.approved!==8||report.tools?.linked!==8||(report.tools?.missing||[]).length)err.push('tool hub coverage');for(const r of approved)if(!tools.includes(`href="/pm-lab/franchise-ssg-preview${r}"`))err.push('tool '+r);if(tools.includes('/tools/store-density/'))err.push('store-density');
if(!tools.includes('data-v25-tools="1"')||!compare.includes('data-v25-multi="1"'))err.push('v25 hubs');
for(const [k,l] of [['candidateHtmlMissing',graph.candidateHtmlMissing],['unreachableCandidates',graph.unreachableCandidates],['orphanCandidates',graph.orphanCandidates],['hubDetailDepthViolations',graph.hubDetailDepthViolations],['allDepthViolations',graph.allDepthViolations]])if((l||[]).length)err.push(`${k}:${l.length}`);if(graph.maxCandidateDepth>3)err.push('max depth '+graph.maxCandidateDepth);if(graph.htmlRouteCount<300)err.push('html count '+graph.htmlRouteCount);
for(const [k,row] of Object.entries(report.hubCoverage||{})){if((row.missing||[]).length||row.expected!==row.linkedCandidates)err.push(`hub ${k} ${row.linkedCandidates}/${row.expected}`)}if(Object.keys(report.hubCoverage||{}).length!==4)err.push('four hubs');
for(const [name,html] of [['home',home],['tools',tools],['categories',categories],['compare',compare]])if(!html.includes('noindex,nofollow,noarchive,nosnippet'))err.push('noindex '+name);const robots=await fs.readFile(path.join(out,'robots.txt'),'utf8');if(robots.trim()!=='User-agent: *\nDisallow: /')err.push('robots');
if(err.length){console.error(JSON.stringify({internalAuthorityV11_25Validation:'FAIL',errors:err},null,2));process.exit(1)}console.log(JSON.stringify({internalAuthorityV11_25Validation:'PASS',candidates:184,maxDepth:graph.maxCandidateDepth,orphans:0,tools:'8/8',hubCoverage:Object.fromEntries(Object.entries(report.hubCoverage).map(([k,v])=>[k,`${v.linkedCandidates}/${v.expected}`]))},null,2));
