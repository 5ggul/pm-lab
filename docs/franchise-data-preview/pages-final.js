'use strict';
function home(){
 setTitle('프랜차이즈 창업 데이터 비교');
 const top=rankBrands('all','stores').slice(0,8).map(brandCard).join('');
 const cats=Object.keys(categories).slice(0,12).map(categoryCard).join('');
 return `<div class="page"><section class="hero"><div class="shell hero-grid"><div><span class="eyebrow">FRANCHISE DATA SEARCH</span><h1>창업 전에,<br><em>브랜드·업종·상권</em>을 한 번에.</h1><p class="hero-copy">외식부터 편의점·교육·미용·세탁·자동차서비스까지 ${Object.keys(categories).length}개 업종, ${brands.length}개 브랜드 규모로 확장한 최종 검수형 프리뷰입니다.</p><div class="search-wrap"><form class="hero-search" id="brandSearch"><input id="brandSearchInput" autocomplete="off" placeholder="브랜드 검색 — 예: 메가커피, 교촌, 크린토피아" aria-label="브랜드 검색"><button class="primary-button">검색</button></form><div class="search-suggestions" id="searchSuggestions"></div></div><div class="hero-chips"><a class="chip" href="#/category/cafe">카페</a><a class="chip" href="#/category/chicken">치킨</a><a class="chip" href="#/category/convenience">편의점</a><a class="chip" href="#/category/study">스터디카페</a><a class="chip" href="#/category/beauty">미용</a></div></div><div class="hero-panel"><div class="panel-top"><div><small>서비스 커버리지</small><h2 class="panel-title">전 업종 통합 창업 데이터</h2></div><span class="demo-badge">NOINDEX PREVIEW</span></div><div class="metric-grid"><div class="metric-mini"><span>업종</span><b>${Object.keys(categories).length}개</b></div><div class="metric-mini"><span>브랜드 디렉터리</span><b>${brands.length}개</b></div><div class="metric-mini"><span>지역 샘플</span><b>${areas.length}곳</b></div><div class="metric-mini"><span>가이드</span><b>${guides.length}개</b></div></div><div class="panel-insight"><b>공식 API 복제 사이트가 아니라</b> 업종 중앙값·성장률·비교·지역밀도·손익 계산까지 자체 가치층을 갖는 구조로 설계했습니다.</div><div class="panel-source"><span>${DATA_MODE}</span><span>정식 공개 전 공식 Snapshot 교체</span></div></div></div></section>
 <section class="section"><div class="shell"><div class="section-head"><div><span class="eyebrow">CATEGORY DIRECTORY</span><h2>창업 업종 전체 보기</h2><p>외식뿐 아니라 소매·서비스 업종까지 한 구조에서 탐색합니다.</p></div><a class="text-link" href="#/categories">20개 업종 전체 →</a></div><div class="category-grid">${cats}</div></div></section>
 <section class="section soft"><div class="shell"><div class="section-head"><div><span class="eyebrow">POPULAR BRANDS</span><h2>규모 기준 상위 브랜드</h2><p>현재는 합성 스냅샷이며, 정식 서비스에서는 공식 가맹점 데이터로 교체됩니다.</p></div><a class="text-link" href="#/brands">브랜드 전체 →</a></div><div class="card-grid">${top}</div></div></section>
 <section class="section"><div class="shell"><div class="section-head"><div><span class="eyebrow">DECISION TOOLS</span><h2>검색에서 계산까지 끊기지 않게</h2></div></div><div class="tool-cards four"><article class="tool-card"><span class="tool-icon">↔</span><h3>브랜드 비교</h3><p>비용·점포·성장·매출지표를 같은 기준으로 비교합니다.</p><a class="text-link" href="#/compare">비교하기 →</a></article><article class="tool-card"><span class="tool-icon">₩</span><h3>창업비용 계산기</h3><p>임대차·권리금·추가설비·운전자금을 합산합니다.</p><a class="text-link" href="#/tools/startup-cost">계산하기 →</a></article><article class="tool-card"><span class="tool-icon">◎</span><h3>상권 밀도</h3><p>전국 주요 지역에서 업종별 공급 밀도를 비교합니다.</p><a class="text-link" href="#/areas">상권 보기 →</a></article><article class="tool-card"><span class="tool-icon">▦</span><h3>손익분기</h3><p>월 매출과 고정·변동비를 넣어 시나리오를 계산합니다.</p><a class="text-link" href="#/tools/break-even">시뮬레이션 →</a></article></div></div></section>
 <section class="section soft"><div class="shell"><div class="section-head"><div><span class="eyebrow">GUIDES</span><h2>데이터를 잘못 읽지 않게 만드는 콘텐츠</h2></div><a class="text-link" href="#/guides">가이드 전체 →</a></div><div class="guide-grid">${guides.slice(0,6).map(g=>guideCard(g)).join('')}</div></div></section></div>`;
}

function categoriesPage(){
 setTitle('창업 업종 전체');
 const groups=['외식','소매','서비스'];
 return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'업종'}])}<div class="page-head"><div><span class="eyebrow">ALL CATEGORIES</span><h1>창업 업종 전체</h1><p>한두 업종만 보여주는 데모가 아니라 외식·소매·서비스를 함께 탐색하는 전체 디렉터리입니다.</p></div></div></div></section><div class="shell content-wrap">${previewNotice()}${groups.map(group=>`<section class="category-section"><div class="section-head compact"><div><h2>${group}</h2><p>${Object.values(categories).filter(c=>c.group===group).length}개 업종</p></div></div><div class="category-grid">${Object.entries(categories).filter(([,c])=>c.group===group).map(([k])=>categoryCard(k)).join('')}</div></section>`).join('')}</div></div>`;
}

function categoryPage(slug){
 const c=categories[slug]||categories.cafe, list=rankBrands(slug,'stores'),s=categoryStats(slug);
 setTitle(`${c.name} 프랜차이즈 비교`);
 return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'업종',href:'/categories'},{label:c.name}])}<div class="page-head"><div><span class="eyebrow">${c.group.toUpperCase()} CATEGORY</span><h1>${c.icon} ${c.name} 프랜차이즈</h1><p>${c.desc}. 업종 안에서 브랜드 규모·창업비용·성장·매출 관련 지표를 같은 기준으로 비교합니다.</p></div><div class="page-actions"><a class="outline-button" href="#/rankings/${slug}/stores">업종 랭킹</a><a class="primary-button" href="#/compare?category=${slug}">브랜드 비교</a></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="summary-grid"><div class="summary-card"><span>브랜드 수</span><b>${s.count}개</b><small>현재 프리뷰 디렉터리</small></div><div class="summary-card"><span>초기비용 중앙값</span><b>${won(s.costMedian)}</b><small>합성 샘플 기준</small></div><div class="summary-card"><span>가맹점 중앙값</span><b>${num(s.storesMedian)}개</b><small>업종 내부 비교</small></div><div class="summary-card"><span>성장률 중앙값</span><b class="${s.growthMedian>=0?'positive':'negative'}">${s.growthMedian>=0?'+':''}${pct(s.growthMedian)}</b><small>전년 대비 샘플</small></div></div>
 <div class="metric-tabs"><a href="#/rankings/${slug}/stores">매장 많은 순</a><a href="#/rankings/${slug}/cost">비용 낮은 순</a><a href="#/rankings/${slug}/growth">성장 높은 순</a><a href="#/rankings/${slug}/sales">매출지표 높은 순</a></div>
 <section class="section-block"><div class="section-head compact"><div><h2>${c.name} 브랜드 전체</h2><p>${list.length}개 브랜드를 모두 표시합니다.</p></div></div><div class="card-grid">${list.map(brandCard).join('')}</div></section>${sourceLine()}</div></div>`;
}

function brandsPage(){
 setTitle('프랜차이즈 브랜드 전체');
 return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'브랜드'}])}<div class="page-head"><div><span class="eyebrow">BRAND DIRECTORY</span><h1>프랜차이즈 브랜드 ${brands.length}개</h1><p>${Object.keys(categories).length}개 업종을 검색·필터·정렬해서 브랜드 상세로 이동합니다.</p></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="panel filter-panel"><div class="form-grid three"><div class="field"><label>브랜드 검색</label><input id="listSearch" placeholder="브랜드명 검색"></div><div class="field"><label>업종</label><select id="categoryFilter"><option value="all">전체 업종</option>${Object.entries(categories).map(([k,v])=>`<option value="${k}">${v.name}</option>`).join('')}</select></div><div class="field"><label>정렬</label><select id="brandSort"><option value="stores">매장 많은 순</option><option value="cost">초기비용 낮은 순</option><option value="growth">성장 높은 순</option><option value="sales">매출지표 높은 순</option><option value="name">이름순</option></select></div></div></div><div class="section-head compact directory-count"><div><h2>검색 결과 <span id="brandCount">${brands.length}</span>개</h2><p id="brandScope">전체 업종</p></div></div><div class="card-grid" id="brandGrid">${rankBrands('all','stores').map(brandCard).join('')}</div></div></div>`;
}

function brandPage(slug){
 const b=bySlug(slug),d=derived(b),c=categories[b.categorySlug],peers=rankBrands(b.categorySlug,'stores').filter(x=>x.slug!==b.slug).slice(0,5),maxRegion=Math.max(...Object.values(b.regions));
 setTitle(`${b.name} 창업비용·가맹 현황`);
 return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'브랜드',href:'/brands'},{label:c.name,href:`/category/${b.categorySlug}`},{label:b.name}])}<div class="page-head"><div><span class="eyebrow">${c.icon} ${c.name}</span><h1>${esc(b.name)} 창업 데이터</h1><p>비용·점포수·성장·매출 관련 지표·지역 분포·동종업계 상대 위치를 한 페이지에서 확인합니다.</p></div><div class="page-actions"><a class="outline-button" href="#/compare/${b.slug}/${peers[0]?.slug||brands[1].slug}">1:1 비교</a><a class="primary-button" href="#/tools/startup-cost?brand=${b.slug}">내 조건 계산</a></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="summary-grid"><div class="summary-card"><span>초기비용 샘플</span><b>${won(b.cost)}</b><small>임대차·권리금 별도</small></div><div class="summary-card"><span>가맹점 샘플</span><b>${num(b.stores)}개</b><small>직영점 ${num(b.direct)}개</small></div><div class="summary-card"><span>매출 관련 지표</span><b>${won(b.sales)}</b><small>연 단위 합성값</small></div><div class="summary-card"><span>전년 대비 점포</span><b class="${d.yoy>=0?'positive':'negative'}">${d.yoy>=0?'+':''}${pct(d.yoy)}</b><small>${num(b.lastStores)} → ${num(b.stores)}</small></div></div>
 <div class="insight-banner"><p><b>${c.name} 업종 중앙값 대비</b> 초기비용은 ${Math.abs(d.costVs).toFixed(1)}% ${d.costVs<0?'낮고':'높고'}, 점포 수는 ${Math.abs(d.storeVs).toFixed(1)}% ${d.storeVs>=0?'많으며':'적으며'}, 매출 관련 지표는 ${Math.abs(d.salesVs).toFixed(1)}% ${d.salesVs>=0?'높습니다':'낮습니다'}.</p><span class="insight-label">자체 파생값</span></div>
 <div class="two-col"><div class="stack"><section class="panel"><h2>초기비용 구성</h2><p class="panel-sub">가맹비·교육비·보증금·인테리어·기타 공개비용을 분리합니다.</p><div class="bar-list">${[['가맹비',b.fee],['교육비',b.education],['보증금',b.deposit],['인테리어',b.interior],['기타',b.other]].map(([l,v])=>`<div class="bar-row"><span class="bar-label">${l}</span><span class="bar-track"><span class="bar-fill" style="width:${Math.max(4,v/b.cost*100)}%"></span></span><span class="bar-value">${won(v)}</span></div>`).join('')}</div>${sourceLine()}</section>
 <section class="panel"><h2>점포 변화와 구조</h2><div class="data-table-wrap"><table class="data-table"><thead><tr><th>지표</th><th class="num">이전</th><th class="num">현재</th><th class="num">해석</th></tr></thead><tbody><tr><td>가맹점</td><td class="num">${num(b.lastStores)}</td><td class="num"><b>${num(b.stores)}</b></td><td class="num"><span class="trend ${d.yoy>=0?'up':'down'}">${d.yoy>=0?'▲':'▼'} ${pct(Math.abs(d.yoy))}</span></td></tr><tr><td>직영점</td><td class="num">—</td><td class="num">${num(b.direct)}</td><td class="num">공식 연결 예정</td></tr><tr><td>가맹점 비율</td><td class="num">—</td><td class="num"><b>${pct(d.ratio)}</b></td><td class="num">자체 계산</td></tr></tbody></table></div>${sourceLine()}</section>
 <section class="panel"><h2>전국 지역 분포</h2><p class="panel-sub">17개 시도에 대한 분포 UI까지 검수합니다.</p><div class="bar-list region-bars">${Object.entries(b.regions).sort((a,z)=>z[1]-a[1]).map(([k,v])=>`<div class="bar-row"><span class="bar-label">${k}</span><span class="bar-track"><span class="bar-fill" style="width:${v/maxRegion*100}%"></span></span><span class="bar-value">${num(v)}개</span></div>`).join('')}</div>${sourceLine('공식 점포주소 + 행정구역 매핑 예정')}</section>
 <section class="panel"><h2>같은 업종 브랜드</h2><div class="data-table-wrap"><table class="data-table"><thead><tr><th>브랜드</th><th class="num">비용</th><th class="num">점포</th><th class="num">증감</th><th></th></tr></thead><tbody>${rankBrands(b.categorySlug,'stores').map(x=>{const dx=derived(x);return `<tr><td><a class="table-link" href="#/brand/${x.slug}">${esc(x.name)}</a></td><td class="num">${won(x.cost)}</td><td class="num">${num(x.stores)}</td><td class="num"><span class="trend ${dx.yoy>=0?'up':'down'}">${dx.yoy>=0?'+':''}${pct(dx.yoy)}</span></td><td><a class="text-link" href="#/compare/${b.slug}/${x.slug}">비교</a></td></tr>`}).join('')}</tbody></table></div>${sourceLine()}</section></div>
 <aside class="stack side-card"><section class="panel"><h3>업종 상대 위치</h3><div class="fact-list"><div class="fact-row"><span>비용 중앙값</span><b>${won(d.medCost)}</b></div><div class="fact-row"><span>점포 중앙값</span><b>${num(d.medStores)}개</b></div><div class="fact-row"><span>매출지표 중앙값</span><b>${won(d.medSales)}</b></div></div></section><section class="cta-box"><h3>이 숫자를 내 조건으로</h3><p>임대보증금·권리금·추가설비·운전자금을 더해 총 필요자금을 계산하세요.</p><a class="primary-button" href="#/tools/startup-cost?brand=${b.slug}">창업비용 계산</a></section><section class="panel"><h3>관련 가이드</h3><div class="link-list">${guides.slice(0,5).map(g=>`<a href="#/guide/${g.slug}">${g.title}<span>→</span></a>`).join('')}</div></section></aside></div></div></div>`;
}

function comparePage(aSlug,bSlug){
 const r=route(),preferred=r.query.get('category'),fallback=preferred?catBrands(preferred):brands,a=bySlug(aSlug||fallback[0]?.slug),same=catBrands(a.categorySlug).filter(x=>x.slug!==a.slug),b=bySlug(bSlug||same[0]?.slug||brands[1].slug),da=derived(a),db=derived(b);
 setTitle(`${a.name} vs ${b.name} 비교`);
 const rows=[['초기비용',won(a.cost),won(b.cost),`${won(Math.abs(a.cost-b.cost))} 차이`],['가맹점 수',`${num(a.stores)}개`,`${num(b.stores)}개`,`${num(Math.abs(a.stores-b.stores))}개 차이`],['전년 대비',`${da.yoy>=0?'+':''}${pct(da.yoy)}`,`${db.yoy>=0?'+':''}${pct(db.yoy)}`,`${pct(Math.abs(da.yoy-db.yoy))}p 차이`],['매출 관련 지표',won(a.sales),won(b.sales),`${won(Math.abs(a.sales-b.sales))} 차이`],['직영점',`${num(a.direct)}개`,`${num(b.direct)}개`,`${num(Math.abs(a.direct-b.direct))}개 차이`]];
 return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'브랜드 비교'}])}<div class="page-head"><div><span class="eyebrow">SIDE BY SIDE</span><h1>${esc(a.name)} vs ${esc(b.name)}</h1><p>비용 하나만 보지 않고 점포 규모·성장·매출 관련 지표까지 같은 표에서 비교합니다.</p></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="compare-builder panel"><div class="field"><label>브랜드 A</label><select id="compareA">${brands.map(x=>`<option value="${x.slug}" ${x.slug===a.slug?'selected':''}>${x.name} · ${x.category}</option>`).join('')}</select></div><span class="vs-mark">VS</span><div class="field"><label>브랜드 B</label><select id="compareB">${brands.map(x=>`<option value="${x.slug}" ${x.slug===b.slug?'selected':''}>${x.name} · ${x.category}</option>`).join('')}</select></div></div><div class="compare-heads"><div class="compare-brand">${avatar(a)}<div><span>${a.category}</span><h2>${esc(a.name)}</h2></div></div><div class="compare-brand">${avatar(b)}<div><span>${b.category}</span><h2>${esc(b.name)}</h2></div></div></div><div class="data-table-wrap"><table class="data-table compare-table"><thead><tr><th>항목</th><th class="num">${esc(a.name)}</th><th class="num">${esc(b.name)}</th><th class="num">차이</th></tr></thead><tbody>${rows.map(r=>`<tr><td><b>${r[0]}</b></td><td class="num">${r[1]}</td><td class="num">${r[2]}</td><td class="num">${r[3]}</td></tr>`).join('')}</tbody></table></div><div class="insight-banner"><p><b>비교 해석.</b> 초기비용은 ${a.cost<b.cost?a.name:b.name} 쪽이 낮고, 현재 점포 수는 ${a.stores>b.stores?a.name:b.name} 쪽이 많습니다. 성장률은 ${da.yoy>db.yoy?a.name:b.name} 쪽이 높습니다. 실제 선택은 계약조건·상권·운영비를 함께 확인해야 합니다.</p><span class="insight-label">자동 비교</span></div>${sourceLine()}</div></div>`;
}

function rankingPage(cat='cafe',metric='stores'){
 if(!categories[cat])cat='cafe'; const c=categories[cat],list=rankBrands(cat,metric),metricName={stores:'가맹점 많은 순',cost:'초기비용 낮은 순',growth:'성장률 높은 순',sales:'매출지표 높은 순'}[metric]||'가맹점 많은 순';setTitle(`${c.name} ${metricName}`);
 return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'업종',href:'/categories'},{label:c.name,href:`/category/${cat}`},{label:'랭킹'}])}<div class="page-head"><div><span class="eyebrow">CATEGORY RANKING</span><h1>${c.name} ${metricName}</h1><p>자의적인 추천 점수가 아니라 선택한 데이터 지표 기준으로만 정렬합니다.</p></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="ranking-controls"><select id="rankingCategory">${Object.entries(categories).map(([k,v])=>`<option value="${k}" ${k===cat?'selected':''}>${v.name}</option>`).join('')}</select><div class="metric-tabs"><a class="${metric==='stores'?'active':''}" href="#/rankings/${cat}/stores">매장수</a><a class="${metric==='cost'?'active':''}" href="#/rankings/${cat}/cost">비용</a><a class="${metric==='growth'?'active':''}" href="#/rankings/${cat}/growth">성장</a><a class="${metric==='sales'?'active':''}" href="#/rankings/${cat}/sales">매출지표</a></div></div><div class="data-table-wrap"><table class="data-table"><thead><tr><th>순위</th><th>브랜드</th><th class="num">초기비용</th><th class="num">가맹점</th><th class="num">성장률</th><th class="num">매출지표</th></tr></thead><tbody>${list.map((b,i)=>{const d=derived(b);return `<tr><td><span class="rank ${i<3?'top':''}">${i+1}</span></td><td><a class="brand-cell table-link" href="#/brand/${b.slug}">${avatar(b)}<span><b>${esc(b.name)}</b><small>${b.category}</small></span></a></td><td class="num">${won(b.cost)}</td><td class="num">${num(b.stores)}개</td><td class="num"><span class="trend ${d.yoy>=0?'up':'down'}">${d.yoy>=0?'+':''}${pct(d.yoy)}</span></td><td class="num">${won(b.sales)}</td></tr>`}).join('')}</tbody></table></div>${sourceLine()}</div></div>`;
}

function areasPage(){
 setTitle('전국 창업 상권');
 const sido=[...new Set(areas.map(a=>a.sido))];
 return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'상권 분석'}])}<div class="page-head"><div><span class="eyebrow">TRADE AREA</span><h1>전국 주요 상권 ${areas.length}곳</h1><p>17개 시도 주요 지역에서 ${Object.keys(categories).length}개 업종의 공급 밀도를 같은 방식으로 비교합니다.</p></div><div class="page-actions"><a class="primary-button" href="#/tools/store-density">반경 밀도 계산기</a></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="panel filter-panel"><div class="form-grid three"><div class="field"><label>시도</label><select id="areaSido"><option value="all">전국</option>${sido.map(s=>`<option>${s}</option>`).join('')}</select></div><div class="field"><label>업종</label><select id="areaCategory">${Object.entries(categories).map(([k,v])=>`<option value="${k}">${v.name}</option>`).join('')}</select></div><div class="field"><label>지역 검색</label><input id="areaSearch" placeholder="강남구, 해운대구, 전주시"></div></div></div><div class="area-grid" id="areaGrid">${areas.map(a=>areaCard(a,'cafe')).join('')}</div></div></div>`;
}
function areaCard(a,cat='cafe'){const c=categories[cat],cnt=a.categoryCounts[cat],density=cnt/a.areaKm2;return `<article class="area-card" data-area="${a.slug}" data-cat="${cat}" tabindex="0"><div><span class="category-tag">${a.sido}</span><h3>${a.name}</h3><p>${c.icon} ${c.name}</p></div><div class="area-metrics"><div><span>업종 업소</span><b>${num(cnt)}곳</b></div><div><span>㎢당</span><b>${density.toFixed(1)}곳</b></div><div><span>전체 대비</span><b>${pct(cnt/a.allStores*100)}</b></div></div><a class="text-link" href="#/area/${a.slug}/${cat}">상세 보기 →</a></article>`}
function areaDetailPage(slug,cat='cafe'){const a=areaBySlug(slug);if(!categories[cat])cat='cafe';const c=categories[cat],cnt=a.categoryCounts[cat],density=cnt/a.areaKm2,ranked=Object.entries(a.categoryCounts).sort((x,y)=>y[1]-x[1]);setTitle(`${a.sido} ${a.name} ${c.name} 상권`);return `<div class="page"><section class="page-hero"><div class="shell">${crumb([{label:'상권 분석',href:'/areas'},{label:a.sido},{label:a.name}])}<div class="page-head"><div><span class="eyebrow">${a.sido} · TRADE AREA</span><h1>${a.name} ${c.name} 상권</h1><p>업종 개수·면적당 밀도·지역 내 비중을 보여주는 공급 측 분석입니다.</p></div></div></div></section><div class="shell content-wrap">${previewNotice()}<div class="category-switch">${Object.entries(categories).map(([k,v])=>`<a class="${k===cat?'active':''}" href="#/area/${a.slug}/${k}">${v.icon} ${v.name}</a>`).join('')}</div><div class="summary-grid"><div class="summary-card"><span>${c.name} 업소</span><b>${num(cnt)}곳</b></div><div class="summary-card"><span>면적당 밀도</span><b>${density.toFixed(1)}곳/㎢</b></div><div class="summary-card"><span>전체 상가 대비</span><b>${pct(cnt/a.allStores*100)}</b></div><div class="summary-card"><span>외식 업소 전체</span><b>${num(a.foodStores)}곳</b></div></div><div class="two-col"><div class="stack"><section class="panel"><h2>업종 구성</h2><div class="bar-list">${ranked.slice(0,12).map(([k,v])=>`<div class="bar-row"><span class="bar-label">${categories[k].name}</span><span class="bar-track"><span class="bar-fill" style="width:${v/ranked[0][1]*100}%"></span></span><span class="bar-value">${num(v)}</span></div>`).join('')}</div>${sourceLine('상가(상권)정보 API 연결 예정')}</section><section class="panel"><h2>${c.name} 세부 상권 구간</h2><p class="panel-sub">현재는 레이아웃 검수용 구간명이며 실제 서비스에서는 법정동/상권경계로 교체됩니다.</p><div class="data-table-wrap"><table class="data-table"><thead><tr><th>구간</th><th class="num">업소</th><th class="num">비중</th></tr></thead><tbody>${a.topZones.map(([z,v])=>`<tr><td>${z}</td><td class="num">${num(Math.round(v*categoryDensity[cat]/categoryDensity.cafe))}곳</td><td class="num">${pct(v/cnt*100)}</td></tr>`).join('')}</tbody></table></div>${sourceLine()}</section></div><aside class="stack side-card"><section class="cta-box"><h3>내 주소 반경으로 계산</h3><p>300m·500m·1km 반경 밀도 도구 UI도 구현되어 있습니다.</p><a class="primary-button" href="#/tools/store-density?area=${a.slug}&category=${cat}">밀도 계산기</a></section><section class="panel"><h3>주의</h3><p class="panel-sub">업종 수가 많다는 사실만으로 수요·매출·성공 가능성을 판단할 수 없습니다. 유동인구와 배후수요는 별도 데이터입니다.</p></section></aside></div>${sourceLine()}</div></div>`}

function guideCard(g){return `<a class="guide-card" href="#/guide/${g.slug}"><div><span class="eyebrow">GUIDE</span><h3>${g.title}</h3><p>${g.summary}</p></div><div class="guide-tags">${g.tags.map(t=>`<span>${t}</span>`).join('')}</div></a>`}
