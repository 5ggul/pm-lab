# 내차데이터 통합 작업 현황 — 2026-09-12

## 이번 묶음에서 완료

- 차량 대표 사진 383/422. 출처·저작자·라이선스와 반응형 WebP 3종을 함께 관리한다.
- BMW i7·iX의 사람이 보이는 전시장 사진 교체.
- 봉고·포터·라보 기반 특장 전기차 6개에 기반 차체 사진과 차이 고지 추가.
- 무사진 카드와 미리보기의 대체 화면 정리. `준비 중` 문구 제거.
- 계산 가이드 7개 추가. 기존 8개를 포함해 공개 가이드 15개.
- 가이드 생성 단계를 CI에 추가해 생성 페이지가 다음 배포에서 사라지지 않도록 고정.
- 계산기 첫 화면은 애니메이션 중에도 즉시 읽히도록 수정.
- K5·쏘나타, 모닝·캐스퍼, 레이·캐스퍼 비교와 전기차 비교 4개를 추가해 정적 비교 25개.
- 전기차 비교는 km/kWh, 1회 충전 주행거리, 충전단가, 전기 승용차 정상세액 13만원을 사용한다.
- 비교 허브는 가솔린·하이브리드·전기차로 구분하고 홈은 대표 비교 9개만 표시한다.
- 422개 차량 상세, 비교 25개, 순위 8개, 도구 5개, 리콜 5개 유지.
- 제조사 공식 차체 분류를 3개에서 25개로 확대하고 SUV·세단·전기 SUV 순위를 분리했다.
- `/rankings/` 통합 허브를 추가해 연료별 효율·차체 형태별·연간 비용의 8개 순위를 한곳에서 찾을 수 있게 했다.
- 각 순위의 현재 1위 차량, 대표 사진, 수치와 비교 차종 수를 허브의 정적 HTML에 표시했다.
- 모든 상단 메뉴의 `연비 순위` 링크를 전체 범위를 보여주는 `순위` 메뉴로 통일했다.
- 정적 HTML 128개, 내부 참조 5,289개, 끊어진 내부 링크 0개를 확인했다.
- 페이지 종류별 최종 색인 품질 게이트를 추가했다. 출시 가능 119개, 내부·호환 경로 제외 8개, 리디렉션 1개, 실패 0개다.
- 차량 상세 35개에 `Vehicle` 구조화 데이터를 맞추고, 비교 페이지 두 곳에서 한쪽 차량 사진이 빠지던 연결 오류를 수정했다.
- 데이터 출처 페이지의 깨진 조사 3곳과 순위 허브 구조화 데이터의 붙어 쓰인 제목을 수정하고 회귀 검사에 추가했다.
- G90·스타리아·아이오닉 6·아이오닉 9·캐스퍼·코나 공식 제원을 추가해 정적 차량 상세 35개 모두 제조사 제원과 연결했다.
- 현대 25개, 기아 24개, 제네시스 10개 차량을 제조사별 정적 목록에 사진·사양 수·복합 효율과 함께 표시했다.
- 원문 이름 전용으로 남아 있던 36개 차종을 원본 제조사·차명과 일치하는 명시 규칙으로 정리했다. 원본 4,203행은 그대로이며 현재 `raw_only` 차종은 0개다.
- 세대를 확인할 근거가 없는 공시 사양은 화면에서 내부 표현 대신 `연식 통합`으로 표시한다. 세대 코드를 추정하지 않는다.

## 출시 전 유지 조건

- 오피넷 수집 실패 시 마지막 정상 수집일 또는 직접 입력 상태를 표시한다.
- 차량 상세와 순위의 숫자는 JavaScript 없이도 읽을 수 있어야 한다.
- 공개 문구에 내부 작업 용어와 `준비 중`을 노출하지 않는다.
- 사진은 차종·세대 연결과 상업 이용 조건을 확인한 것만 사용한다.
- `noindex,nofollow,noarchive`는 최종 도메인 전환 전까지 유지한다.
- `data/index-quality-gate.json`의 실패가 0개인 경우에만 배포한다.

## 외부 근거가 생길 때만 갱신

- 대표 사진 39개: Wikimedia Commons 전체 후보를 다시 검색했다. 정확한 후보 1건은 관람객이 다수 보이는 전시장 사진이어서 제외했고, 나머지는 동명이인·문서·무관 차량이었다. 각 차종의 미확보 사유를 `photo-expansion-audit.json`에 기록한다.
- 세대 코드 근거가 없는 367개 공시 그룹: 데이터에는 미분류 상태를 유지하고 공개 화면에서는 여러 연식이 섞였음을 뜻하는 `연식 통합`으로 표시한다. 제조사 자료나 차대 코드가 확인될 때만 분리한다.
- 제조사 공식 차체 제원은 정적 상세 대상 35개 전부 연결했다. 나머지 387개는 한국에너지공단 공식 연비·전비 사양만 제공하며 제3자 제원을 복사하거나 추정하지 않는다.

## 도메인 연결 때 실행

1. 최종 도메인과 모든 canonical을 한 번에 변경한다.
2. 운영용 robots.txt와 sitemap.xml을 생성한다.
3. `data/index-quality-gate.json`의 출시 가능 119개, 제외 8개, 리디렉션 1개를 최종 도메인 기준으로 다시 생성한다.
4. 승인된 페이지만 색인 허용으로 전환한다.
5. Search Console 등록과 색인 요청 후 AdSense 심사를 진행한다.
