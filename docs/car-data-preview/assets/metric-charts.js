(function(root){
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const number=v=>v.toLocaleString('ko-KR',{maximumFractionDigits:2});
 function bars({title,rows,unit='원',note='',stacked=false,energyTerm='연료·충전비'}){
  if(rows.length!==2||rows.some(r=>!r.values.length||r.values.some(v=>!Number.isFinite(v)||v<0))||stacked&&rows.some(r=>r.values.length!==2))return '';
  const totals=rows.map(r=>r.values.reduce((a,b)=>a+b,0)),cost=unit==='원',middle=stacked?rows[1].values[0]+rows[0].values[1]:0;
  const max=Math.max(...totals,middle,1);if(!Number.isFinite(max))return '';
  const gap=Math.abs(totals[0]-totals[1]),same=cost?Math.round(gap)===0:gap<.00001,winner=cost?(totals[0]<totals[1]?0:1):(totals[0]>totals[1]?0:1);
  const money=v=>'약 '+Math.round(v).toLocaleString('ko-KR')+'원';
  const display=v=>cost?money(v):number(v)+' '+unit;
  const compact=v=>cost?(v<10000?Math.round(v).toLocaleString('ko-KR')+'<small>원</small>':number(Math.round(v/1000)/10)+'<small>만원</small>'):number(v)+'<small>'+esc(unit)+'</small>';
  const names=rows.map(r=>r.label.replace(/^[AB]\s*/,''));
  const partDiff=stacked?rows[0].values.map((v,i)=>v-rows[1].values[i]):[];
  const reason=stacked?(partDiff.every(v=>Math.abs(v)<.5)?'세금과 '+energyTerm+'가 같습니다.':Math.abs(partDiff[1])<.5?'세금은 같고, '+energyTerm+'에서 갈립니다.':Math.abs(partDiff[0])<.5?energyTerm+'는 같고, 세금에서 갈립니다.':energyTerm+'와 세금, 두 차이를 봅니다.'):(cost?'같은 거리를 달릴 때의 에너지비입니다.':unit==='km/kWh'?'1kWh로 가는 거리':'1L로 가는 거리');
  function endpoint(i){return '<div class="metric-row" data-metric-total="'+totals[i]+'"><div class="metric-total" aria-label="'+esc(display(totals[i]))+'">'+compact(totals[i])+'</div><div class="metric-track" aria-hidden="true">'+rows[i].values.map((v,j)=>'<span class="metric-fill metric-part-'+j+'" data-value="'+v+'" style="height:'+v/max*100+'%"></span>').join('')+'</div><span class="metric-end-label">'+(i?'B':'A')+' '+(cost?'총비용':unit==='km/kWh'?'전비':'연비')+'</span></div>'}
  function change(i){const before=i?middle:totals[0],after=i?totals[1]:middle,diff=after-before,zero=Math.abs(diff)<.5;return '<div class="metric-change" data-change="'+diff+'" data-before="'+before+'" data-after="'+after+'"><div class="metric-change-value '+(diff>0?'is-plus':'')+'">'+(zero?'동일':(diff>0?'+':'−')+compact(Math.abs(diff)))+'</div><div class="metric-change-track" style="--before-y:'+before/max*100+'%;--after-y:'+after/max*100+'%" aria-hidden="true"><span class="metric-bridge '+(diff>0?'is-plus':'')+(zero?' is-zero':'')+'" style="bottom:'+Math.min(before,after)/max*100+'%;height:'+Math.abs(diff)/max*100+'%"></span></div><span class="metric-end-label">'+(i?'자동차세<br>차이':esc(energyTerm)+'<br>차이')+'</span></div>'}
  return '<figure class="metric-chart metric-editorial '+(stacked?'metric-waterfall':'metric-pair')+'" data-metric-unit="'+esc(unit)+'" data-metric-max="'+max+'"><figcaption><span class="metric-eyebrow">'+esc(title)+'</span><h3>'+esc(reason)+'</h3><div class="metric-verdict">'+(same?'<strong>'+(cost?'연간 비용':'복합 효율')+'이 같습니다</strong>':'<span>'+(winner?'B':'A')+'가 '+(cost?'연간':'')+'</span><strong>'+(cost?'<small>약</small> ':'')+compact(gap)+'</strong><span>'+(cost?'적습니다':'더 높습니다')+'</span>')+'</div></figcaption><div class="metric-vehicles">'+rows.map((r,i)=>'<div class="metric-vehicle" data-metric-side="'+i+'"><span class="metric-letter">'+(i?'B':'A')+'</span><span>'+esc(names[i])+'</span></div>').join('')+'</div><div class="metric-rows">'+endpoint(0)+(stacked?change(0)+change(1):'')+endpoint(1)+'</div><div class="metric-baseline" aria-hidden="true"><span>0</span><span>'+(stacked?'A에서 B로 바꿀 때의 차이':esc(unit))+'</span></div>'+(stacked?'<p class="metric-reading">가운데 막대는 비용이 줄거나 늘어나는 부분입니다. '+(partDiff[0]>=0?energyTerm+' 감소':energyTerm+' 증가')+' '+money(Math.abs(partDiff[0]))+' · '+(Math.abs(partDiff[1])<.5?'자동차세 동일':'자동차세 '+(partDiff[1]>0?'감소 ':'증가 ')+money(Math.abs(partDiff[1])))+'</p>':'')+'<details class="metric-detail"><summary>'+(cost?'정확한 금액과 계산 기준':'수치와 비교 기준')+'</summary><div class="metric-detail-values">'+rows.map((r,i)=>'<p><b>'+(i?'B':'A')+'</b> '+display(totals[i])+(stacked?'<span>연료·충전비 '+money(r.values[0])+' / 자동차세 '+Math.round(r.values[1]).toLocaleString('ko-KR')+'원</span>':'')+'</p>').join('')+'</div><p>'+esc(note)+'</p></details></figure>';
 }
 root.CAR_METRIC_CHARTS={bars};
})(globalThis);
